# Core PPT functionality
python-pptx==1.0.2
python-docx>=0.8.11
Pillow>=10.0.0

# Web UI
gradio>=6.0.0

# Azure OpenAI & LangChain
openai>=1.0.0
langchain-openai>=0.2.0
langchain-core>=0.3.0
python-dotenv>=1.0.0

# Logging
loguru>=0.7.0

# Audio transcription (Whisper) and Vision Models
# Note: For CUDA support on standard GPUs (up to sm_90):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# For Blackwell GPUs (sm_120+) like RTX PRO 3000, use PyTorch nightly:
#   pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu124
#
# See docs/BLACKWELL_GPU_SETUP.md for detailed instructions
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.20.0
bitsandbytes>=0.43.1


